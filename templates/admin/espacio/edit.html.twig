{% extends 'base.html.twig' %}

{% block title %}Edit Espacio{% endblock %}

{% block actions %}
    <div class="btn-group me-2">
        <a href="{{ path('app_admin_espacio_index') }}" class="btn btn-sm me-2">Volver a la lista</a>
        <a class="btn btn-sm btn-outline-secondary">Borrar</a>
    </div>
{% endblock %}

{% block body %}
    <h1>Edit Espacio</h1>

    {{ include('admin/espacio/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_admin_espacio_index') }}">back to list</a>

    {{ include('admin/espacio/_delete_form.html.twig') }}
{% endblock %}
